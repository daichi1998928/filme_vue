<!DOCTYPE html>
<html>
  <head>
    <title>Filme</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <p><%= link_to "ログアウト (アプリケーション.htmlに書いている)", destroy_user_session_path, method: :delete %></p>
      <p><%= link_to "マイページ(アプリケーション.htmlに書いている)",user_products_path %></p>
      <p><%= link_to "カートの中身(アプリケーション.htmlに書いている)","#" %></p>
      <p><%= link_to "ログイン(アプリケーション.htmlに書いている)",new_user_registration_path %></p>
      <p><%= link_to "新規登録(アプリケーション.htmlに書いている)",new_user_registration_path %></p>
    </div>

    <header>
      <% if flash[:notice] %>
        <%= flash[:notice] %>
      <% end %>

      <p class="header_title">Filme</p>
        <% if user_signed_in? %>
          <li>
            <span><%= link_to "マイページ",user_products_path %></span>
            <span><%= link_to "カートの中身","#" %></span>
            <span><%= link_to "ログアウト",destroy_user_session_path, method: :delete %></span>
          </li>
        <% elsif admin_signed_in? %>
            <span><%= link_to "ログイン",new_user_registration_path %></span>
            <span><%= link_to "新規登録",new_user_registration_path %></span>
        <% else %>

        <% end %>
    </header>
    <%= yield %>
    <footer>
      <small>© CIRCUS</small>
    </footer>
    </div>
  </body>
</html>
