<!DOCTYPE html>
<html>
  <head>
    <title>Filme</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
    <p><%= link_to "ログアウト (アプリケーション.htmlに書いている)", destroy_user_session_path %></p>
    <% if flash[:notice] %>
     <%= flash[:notice] %>
    <% end %>

    <header>
      <h1>Filme</h1>
      <% if user_signed_in? %>
        <li>
          <span><%= link_to "マイページ" %></span>
          <span><%= link_to "カートの中身" %></span>
          <span><%= link_to "ログアウト" %></span>
        </li>

      <% elseif admin_signed_in? %>
          <span><%= link_to "ログイン" %></span>
          <span><%= link_to "新規登録" %></span>

      <% else %>

      <% end %>
    </header>
    <%= yield %>
    <footer>
      <small>© CIRCUS</small>
    </footer>
    </div>
  </body>
</html>
