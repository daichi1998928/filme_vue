<div class="row">
  <div class="col-lg-3">
    <%= render 'user/users/sidebar',user:current_user,edit:false,f:f %>
  </div>

  <div class="col-lg-9">
    <h2 class="text-center profil-title">見た事ある</h2>
    <%= link_to 'Twitter', "https://twitter.com/share?url=#{request.url}", title: 'Twitter', target: '_blank' %>
    <%= link_to 'Facebook', "https://www.facebook.com/sharer/sharer.php?u=#{request.url}", title: 'Facebook', target: '_blank' %>
   <% @movie_reviews.each do |f| %>
   #修正する
    <div class="product-block">
     <%= link_to(user_product_path(product.id)) do %>
      <%= attachment_image_tag product, :image,:size => "200x200" %>
     <div><%= product.product_title %></div>
    <% end %>
    </div>
    <% end %>
  </div>

</div>